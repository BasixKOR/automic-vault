# envchain Radioisotope

Detect-only coverage for envchain-managed secret namespaces.

envchain is itself a keychain-backed environment injector. This radioisotope
reports obvious namespace setup from shell history without moving platform
keychain or Secret Service entries.
