#!/bin/sh

cd "$(dirname "$0")/../.."

git push origin main
git -C data/radioisotopes/ push origin main
git -C data/isotopes/gh-cli/ push origin trunk
git -C data/isotopes/supabase-cli/ push origin trunk
