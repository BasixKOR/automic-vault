#!/bin/sh

cd "$(dirname "$0")/../.."

git pull origin main --rebase
git -C ../av.db/data/radioisotopes/ pull origin main --rebase
git -C ../av.db/data/isotopes/gh-cli/ pull origin trunk --rebase
git -C ../av.db/data/isotopes/supabase-cli/ pull origin trunk --rebase
